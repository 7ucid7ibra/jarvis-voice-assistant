# =============================================================================
# Jarvis Assistant — Environment Variables
# Copy this file to .env and fill in your values.
# .env is gitignored and must never be committed.
# =============================================================================

# --- Home Assistant (required) -----------------------------------------------
# If set, environment value overrides Keychain/app-stored value.
# Long-lived access token from your HA profile page:
#   HA → Profile → Security → Long-Lived Access Tokens → Create Token
HA_TOKEN=your-long-lived-ha-token-here

# URL of your Home Assistant instance (no trailing slash)
HA_URL=http://192.168.1.100:8123

# --- Telegram (optional) -----------------------------------------------------
# If set, environment values override Keychain/app-stored values.
# Needed only for the "send Telegram message" feature.
# Create a bot via @BotFather and get the chat ID from @userinfobot.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- LLM Provider (optional) -------------------------------------------------
# Default is "ollama" (local, no key needed).
# Other options: openai | gemini | opencode
API_PROVIDER=ollama

# API key — only required for openai, gemini, or opencode providers.
# If set, environment value overrides Keychain/app-stored value.
API_KEY=

# --- Optional overrides (defaults shown) -------------------------------------
# WHISPER_MODEL=base          # tiny | base | small | medium | large
# OLLAMA_MODEL=qwen2.5:0.5b
# ASSISTANT_NAME=JARVIS
# LANGUAGE=en                 # en | de | (unset = auto-detect per utterance)
